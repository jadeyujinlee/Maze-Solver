import java.io.File; 
import java.util.Scanner; 

/**
* class Main 
* Tests the original Maze we wrote in Maze.java of size 30 by 30. 
* Does not scan any files. 
*/ 
class Main {
  public static void main(String[] args) {
    // System.out.println("Running...");
    // Maze maze = new Maze(30, 30);
    
    // MazeViewer viewer = new MazeViewer(maze);
    // maze.solver(maze.getStart()); 
    
  }
}